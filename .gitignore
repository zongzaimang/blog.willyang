/_site
/dist
/dist.tar.gz
/.jekyll-cache
/.bundle

# Obsidian stores local window, tab and search state here.  Keep portable vault
# settings under version control, but do not commit each device's workspace.
/.obsidian/workspace.json
